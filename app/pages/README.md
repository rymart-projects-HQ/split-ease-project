# Pages

File-based routing. Each .vue file becomes a route.

## Structure

- `index.vue` → `/`
- `login.vue` → `/login`
- `groups/index.vue` → `/groups`
- `groups/[id].vue` → `/groups/:id` (dynamic)
